services:
  android-build:
    build: .
    volumes:
      - .:/app
      - gradle-cache:/app/.gradle-home
      - android-keystore:/root/.android
    working_dir: /app
    command: ./gradlew assembleDebug

volumes:
  gradle-cache:
  android-keystore:
